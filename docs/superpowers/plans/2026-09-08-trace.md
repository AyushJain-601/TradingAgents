# Secret-Redacted Trace Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Observe one unchanged MTARTECH analysis and retain inspectable, sanitized evidence.

**Architecture:** A context-scoped JSONL writer sanitizes before disk; a LangChain callback captures graph and model events; explicit dataflow spans capture direct prefetches and vendor selection. A standalone runner owns lifecycle, failure handling, scan and artifact readiness.

**Tech Stack:** Python 3.12, LangChain callbacks, LangGraph, pytest, GitHub Actions.

**Spec:** Approved in the ChatGPT session on 2026-09-08; this file records the fixed baseline and privacy contract.

## Global Constraints

- No third-party tracing service.
- Preserve the approved ticker, date, model and graph settings.
- Redact before writing and fail closed before artifact upload.
- Exclude credentials and provider reasoning.
- Run exactly one paid traced analysis.

---

Approved design: local tests, GitHub execution using its existing secret, short-lived sanitized artifacts. No third-party tracing. Same ticker MTARTECH.NS, date 2026-09-07, DeepSeek deepseek-v4-flash, one research/risk round, 8192 output tokens, two SDK retries, no checkpoint.

Execution: inline in isolated branch codex/mtartech-secret-redacted-trace.

1. Write unittest coverage in tests/test_audit_trace.py for nested redaction, encoded credentials, reasoning exclusion, preserved finance numbers, concurrent parentage, exception spans and publication refusal. Run before implementation.
2. Implement tradingagents/audit.py: Redactor.clean(value), Trace.emit(kind, **fields), trace_scope(trace), observe(name) decorator, publication scan. Use a lock for JSONL writes and ContextVars for parent spans. Unknown objects become type markers, never repr.
3. Implement tradingagents/audit_callbacks.py with model, tool and chain lifecycle callbacks. Keep invocation parameters to an allowlist. Register callbacks at graph invocation so node/tool events propagate. Record visible message content/tool calls and usage; exclude provider reasoning and arbitrary client metadata.
4. Add direct data spans to vendor dispatch, Reddit, StockTwits and verified market snapshot; add structured-output outcome events. No retrieval behavior changes.
5. Add scripts/run_traced_analysis.py with validated fixed baseline request, runtime manifest, output final state/report, fail-closed scan, completion marker. Keep provider console output suppressed. Never upload application result/cache folders.
6. Test fake model graph propagation and direct spans, compare enabled/disabled outputs, run existing relevant tests and lint. Inspect diff before commit.
7. Add branch-scoped manual request workflow, with synthetic tests before secret-bearing step, read-only token, one-day artifact retention and readiness-gated upload. Commit/push trace branch, launch exactly one run through request-file push.
8. Retrieve successful artifacts, verify digests, analyze actual node/tool timeline, source gaps, numerical lineage, fallbacks and usage. State uncaptured HTTP internals/retries explicitly. Save final evidence reference.

Acceptance: secrets never written; trace disabled is inert; direct fetches and graph/model calls retained; failures distinguish partial coverage; one paid run only; no brokerage action.
